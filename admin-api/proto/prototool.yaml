protoc:
  version: 3.8.0
  allow_unused_imports: true

lint:
  group: uber2

generate:
  go_options:
    import_path: github.com/digithun/shio/admin-api/proto

  plugins:
    - name: go
      type: go
      flags: plugins=grpc
      output: ../pkg
